# 🎓 Exam Score Collector (Bot + API)

Прототип сервиса для сбора и хранения баллов ЕГЭ. Сделано в рамках тестового задания.

## 🚀 Технологии
* **Backend:** FastAPI (Asynchronous)
* **Database:** PostgreSQL + SQLAlchemy 2.0 (Async Engine)
* **Migrations:** Alembic (автоматическое управление схемой БД)
* **Bot:** Aiogram 3.x
* **Testing:** Pytest + Asyncio (интеграционные тесты API)
* **Infrastructure:** Docker & Docker Compose

## 🛠 Функционал
- **Регистрация:** Ученики регистрируются через Telegram-бота.
- **Управление баллами:** Добавление и автоматическое обновление (Upsert) баллов по предметам.
- **Просмотр:** Вывод списка всех сохраненных результатов через Telegram-id, чтобы пользователи не могли смотреть баллы знакомых и соблдалась академическая честность
- **Валидация:** Проверка корректности баллов (0-100) на уровне API.
- **UI:** Удобное меню с кнопками и валидация выбора предмета.

## 📦 Как запустить проект

Вам не нужно устанавливать Python или PostgreSQL локально. Всё работает в контейнерах.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/kashanqq/Exam-Score-Collector.git
    cd Exam-Score-Collector
    ```

2.  **Настройте окружение:**
    Создайте файл `.env` из примера. Настройки по умолчанию уже подходят для Docker.
    ```bash
    # Linux / MacOS
    cp .env.example .env

    # Windows
    copy .env.example .env
    ```
    *Опционально: вставьте свой `BOT_TOKEN` в `.env`, если хотите проверить работу бота.*

3.  **Запустите контейнеры:**
    ```bash
    docker-compose up -d --build
    ```
    *При первом запуске автоматически применятся миграции Alembic.*

## 🧪 Тестирование

Проект покрыт интеграционными тестами. Вы можете проверить работоспособность бизнес-логики API (регистрация, сохранение, валидация) одной командой, даже не запуская бота.

Выполните команду внутри контейнера:
```bash
docker-compose exec api pytest

Ожидаемый результат:

tests/test_main.py ...                                      [100%]
=== 3 passed in 0.45s ===
```

📖 Документация API

После запуска документация Swagger будет доступна по адресу: http://localhost:8000/docs


